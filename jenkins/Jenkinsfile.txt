pipeline {
    agent any
	
	 environment {
        mvnHome = tool name: 'maven', type: 'maven'
		mvnCMD = "${mvnHome}/bin/mvn"
    }

	
    stages {
        stage('clean') {
            steps {
                bat "${mvnCMD} -f security/security/pom.xml clean clean test package";
            }
        }
    }
}



